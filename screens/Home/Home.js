import { Text } from 'react-native';
import { DismissKeyboardView } from '../../components';

function Home(props) {
    return (
        <DismissKeyboardView>
            <Text>HOME</Text>
        </DismissKeyboardView>
    );
}

export default Home;
